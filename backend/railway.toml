[build]
builder = "nixpacks"

[deploy]
startCommand = "node --max-old-space-size=512 src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 2
